<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lyh.mapper.LiveRoomMapper">

    <!-- LiveRoomDetailDto的映射结果集 -->
    <resultMap id="LiveRoomDetailVoMap" type="top.lyh.entity.vo.LiveRoomDetailVo">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="coverUrl" column="cover_url"/>
        <result property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="hlsUrl" column="hls_url"/>
        <result property="status" column="status"/>
        <result property="viewCount" column="view_count"/>
        <result property="amountCount" column="amount_count"/>
        <result property="startTime" column="start_time"/>
        <result property="categoryId" column="category_id"/>
    </resultMap>

    <!-- 动态条件查询直播间列表（唯一的核心查询方法） -->
    <select id="selectByCondition" resultMap="LiveRoomDetailVoMap">
        SELECT
        lr.id,
        lr.title,
        lr.cover_url,
        lr.user_id,
        su.user_name,
        su.avatar as anchorAvatar,
        lr.hls_url,
        lr.status,
        lr.view_count,
        lr.amount_count,
        lr.start_time,
        lr.category_id
        FROM live_room lr
        LEFT JOIN sys_user su ON lr.user_id = su.id
        <where>
            <!-- 用户状态筛选 -->
            <if test="query.enabledOnly == null or query.enabledOnly == true">
                AND su.enabled = 1
            </if>

            <!-- 直播间ID -->
            <if test="query.id != null">
                AND lr.id = #{query.id}
            </if>

            <!-- 直播间标题模糊查询 -->
            <if test="query.title != null and query.title != ''">
                AND lr.title LIKE CONCAT('%', #{query.title}, '%')
            </if>

            <!-- 主播用户ID -->
            <if test="query.userId != null">
                AND lr.user_id = #{query.userId}
            </if>

            <!-- 主播用户名模糊查询 -->
            <if test="query.userName != null and query.userName != ''">
                AND su.user_name LIKE CONCAT('%', #{query.userName}, '%')
            </if>

            <!-- 直播状态 -->
            <if test="query.status != null">
                AND lr.status = #{query.status}
            </if>
            <!-- 分类ID查询 -->
            <if test="query.categoryId != null">
                AND lr.category_id = #{query.categoryId}
            </if>
        </where>

        <!-- 排序 -->
        <if test="query.orderBy != null and query.orderBy != ''">
            ORDER BY
            <choose>
                <!-- 综合评分排序：view_count * 5 + amount_count * 1.5 -->
                <when test="query.orderBy == 'score'">
                    (lr.view_count * 5 + lr.amount_count * 1.5)
                </when>
                <when test="query.orderBy == 'view_count'">lr.view_count</when>
                <when test="query.orderBy == 'amount_count'">lr.amount_count</when>
                <when test="query.orderBy == 'start_time'">lr.start_time</when>
            </choose>
            <choose>
                <when test="query.orderType != null and query.orderType.toUpperCase() == 'ASC'">ASC</when>
                <otherwise>DESC</otherwise>
            </choose>
        </if>
        <if test="query.orderBy == null or query.orderBy == ''">
            <!-- 默认按综合评分排序 -->
            ORDER BY (lr.view_count * 5 + lr.amount_count * 1.5) DESC
        </if>

        <!-- 分页 -->
        <if test="limit != null">
            LIMIT #{limit}
            <if test="offset != null">
                OFFSET #{offset}
            </if>
        </if>
    </select>
    <!-- 新增：获取各分类的直播间数量统计 -->
    <select id="selectCategoryRoomCount" resultType="top.lyh.entity.vo.CategoryCountVO">
        SELECT
            c.id,
            c.name,
            COUNT(lr.id) as count
        FROM category c
            LEFT JOIN live_room lr ON c.id = lr.category_id AND lr.status = 1
        GROUP BY c.id, c.name
        ORDER BY c.sort_order
    </select>
    <!-- 统计符合条件的总记录数 -->
    <select id="countByCondition" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM live_room lr
        LEFT JOIN sys_user su ON lr.user_id = su.id
        <where>
            <!-- 用户状态筛选 -->
            <if test="query.enabledOnly == null or query.enabledOnly == true">
                AND su.enabled = 1
            </if>

            <!-- 直播间ID -->
            <if test="query.id != null">
                AND lr.id = #{query.id}
            </if>

            <!-- 直播间标题模糊查询 -->
            <if test="query.title != null and query.title != ''">
                AND lr.title LIKE CONCAT('%', #{query.title}, '%')
            </if>

            <!-- 主播用户ID -->
            <if test="query.userId != null">
                AND lr.user_id = #{query.userId}
            </if>

            <!-- 主播用户名模糊查询 -->
            <if test="query.userName != null and query.userName != ''">
                AND su.user_name LIKE CONCAT('%', #{query.userName}, '%')
            </if>

            <!-- 直播状态 -->
            <if test="query.status != null">
                AND lr.status = #{query.status}
            </if>
            <!-- 分类ID查询 -->
            <if test="query.categoryId != null">
                AND lr.category_id = #{query.categoryId}
            </if>
        </where>
    </select>
</mapper>