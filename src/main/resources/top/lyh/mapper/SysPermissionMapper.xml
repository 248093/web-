<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lyh.mapper.SysPermissionMapper">

    <!-- 通过权限编码查询权限 -->
    <select id="selectByPermCode" resultType="top.lyh.entity.pojo.SysPermission">
        SELECT * FROM sys_permission
        WHERE perm_code = #{permCode}
          AND status = 1
    </select>

    <!-- 通过角色ID查询权限列表（关联 sys_role_permission） -->
    <select id="selectByRoleId" resultType="top.lyh.entity.pojo.SysPermission">
        SELECT p.* FROM sys_permission p
                            INNER JOIN sys_role_permission rp ON p.id = rp.perm_id
        WHERE rp.role_id = #{roleId}
          AND p.status = 1
    </select>

    <!-- 通过用户ID查询权限列表（关联 sys_user_role → sys_role_permission → sys_permission） -->
    <select id="selectByUserId" resultType="top.lyh.entity.pojo.SysPermission">
        SELECT DISTINCT p.* FROM sys_permission p
                                     INNER JOIN sys_role_permission rp ON p.id = rp.perm_id
                                     INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND p.status = 1
          AND (SELECT r.status FROM sys_role r WHERE r.id = ur.role_id) = 1
    </select>

    <!-- 查询所有启用的权限 -->
    <select id="selectAllEnabled" resultType="top.lyh.entity.pojo.SysPermission">
        SELECT * FROM sys_permission
        WHERE status = 1
        ORDER BY parent_id ASC, sort ASC
    </select>

</mapper>