<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.lyh.mapper.SysRoleMapper">

    <!-- 通过角色编码查询角色 -->
    <select id="selectByRoleCode" resultType="top.lyh.entity.pojo.SysRole">
        SELECT * FROM sys_role
        WHERE role_code = #{roleCode}
          AND status = 1
    </select>

    <!-- 通过用户ID查询角色列表（关联 sys_user_role） -->
    <select id="selectByUserId" resultType="top.lyh.entity.pojo.SysRole">
        SELECT r.* FROM sys_role r
                            INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
    </select>

    <!-- 查询所有启用的角色 -->
    <select id="selectAllEnabled" resultType="top.lyh.entity.pojo.SysRole">
        SELECT * FROM sys_role
        WHERE status = 1
        ORDER BY sort ASC
    </select>

</mapper>